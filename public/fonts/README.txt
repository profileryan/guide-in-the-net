Place your supplied Header_Font.ttf file in this folder and keep the filename exactly:

Header_Font.ttf

The app already points to /fonts/Header_Font.ttf. If the file is absent, it falls back to a system monospace font.
